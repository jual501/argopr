apiVersion: v1
kind: Namespace
metadata:
  name: apache-prod
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: apache-index
  namespace: apache-prod
data:
  index.html: |
    <html>
      <head><title>Apache - PROD</title></head>
      <body style="font-family: sans-serif;">
        <h1>Apache - PROD</h1>
        <p>This is the <b>PROD</b> environment.</p>
      </body>
    </html>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: apache
  namespace: apache-prod
  labels:
    app: apache
spec:
  replicas: 1
  selector:
    matchLabels: { app: apache }
  template:
    metadata:
      labels: { app: apache, env: prod }
    spec:
      containers:
        - name: httpd
          image: httpd:2.4.59
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet: { path: "/", port: 80 }
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: html
              mountPath: /usr/local/apache2/htdocs/index.html
              subPath: index.html
      volumes:
        - name: html
          configMap:
            name: apache-index
---
apiVersion: v1
kind: Service
metadata:
  name: apache
  namespace: apache-prod
  labels: { app: apache }
spec:
  type: ClusterIP
  selector: { app: apache }
  ports:
    - name: http
      port: 80
      targetPort: 80
